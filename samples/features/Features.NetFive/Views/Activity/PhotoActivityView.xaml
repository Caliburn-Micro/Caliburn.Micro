<UserControl x:Class="Features.CrossPlatform.Views.Activity.PhotoActivityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Features.CrossPlatform.Views.Activity"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="PhotoRectStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Fill" Value="{StaticResource BackgroundBrush}"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Rectangle Style="{StaticResource PhotoRectStyle}" />
            <Rectangle Style="{StaticResource PhotoRectStyle}" />
            <Rectangle Style="{StaticResource PhotoRectStyle}" />
        </StackPanel>

        <TextBlock x:Name="Caption"/>

    </StackPanel>
</UserControl>
