<UserControl x:Class="Features.CrossPlatform.Views.Events.EventSourceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot">
        <StackPanel>
            <TextBlock Text="source" Style="{StaticResource PhoneTextLargeStyle}"/>
            <TextBlock Text="Name" Style="{StaticResource PhoneTextSubtleStyle}"/>
            <TextBox x:Name="Text" HorizontalAlignment="Stretch"/>
            <Button x:Name="Publish" Content="Publish" Margin="0,12" HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</UserControl>