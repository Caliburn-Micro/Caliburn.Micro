<UserControl x:Class="Features.CrossPlatform.Views.FeatureView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cm="http://www.caliburnproject.org"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot">
        <Button cm:Message.Attach="ShowFeature($dataContext)"  HorizontalContentAlignment="Left" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="Title" Style="{StaticResource PhoneTextNormalStyle}"/>
                <TextBlock x:Name="Description" Style="{StaticResource PhoneTextSubtleStyle}"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>