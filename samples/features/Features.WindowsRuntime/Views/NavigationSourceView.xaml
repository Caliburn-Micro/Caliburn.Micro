<Page
    x:Class="Features.CrossPlatform.Views.NavigationSourceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Features.CrossPlatform.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="navigation" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,20,0,0"/>

        <StackPanel Margin="0,20" Grid.Row="1">
            <TextBox x:Name="Text" Header="Text" />
            <CheckBox x:Name="IsEnabled" Content="Is Enabled"/>
            <Button x:Name="Navigate" Content="Navigate"/>
        </StackPanel>
    </Grid>
</Page>
