<Page
    x:Class="Features.CrossPlatform.Views.NavigationTargetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Features.CrossPlatform.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="navigation" Style="{StaticResource SubheaderTextBlockStyle}" Margin="24,12,24,0"/>

        <StackPanel Grid.Row="1" Margin="24,12">
            <TextBlock x:Name="Text" Style="{StaticResource BodyTextBlockStyle}" />
            <TextBlock x:Name="IsEnabled" Style="{StaticResource BodyTextBlockStyle}" />
        </StackPanel>
    </Grid>
</Page>
