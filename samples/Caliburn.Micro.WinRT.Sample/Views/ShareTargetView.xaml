<UserControl
    x:Class="Caliburn.Micro.WinRT.Sample.Views.ShareTargetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Caliburn.Micro.WinRT.Sample.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20">
            <TextBlock Text="Uri" Style="{StaticResource ItemTextStyle}" />
            <TextBox x:Name="Uri" Margin="0,10,0,20" />
            <TextBlock Text="Text" Style="{StaticResource ItemTextStyle}" />
            <TextBox x:Name="Text" Margin="0,10,0,20" />
            <Button x:Name="Share" Content="Share" />
        </StackPanel>
    </Grid>
</UserControl>
